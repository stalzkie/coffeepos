import 'package:flutter/material.dart';

class AppColors {
  // Base colors
  static const Color primary = Color(0xFF1E1E1E); // dark header bg
  static const Color secondary = Color(0xFFE7E7E9); // light background
  static const Color accent = Color(0xFF62FF6D); // green confirm button
  static const Color warning = Color(0xFFFF3838); // red button
  static const Color neutral = Color(0xFFCCCCCC); // placeholder image

  // Text colors
  static const Color textDark = Color(0xFF1E1E1E);
  static const Color textLight = Colors.white;

  // Input field background
  static const Color inputBackground = Color(0xFF525252);

  // Transparent overlay for inactive states
  static const Color muted = Color.fromARGB(128, 255, 255, 255); // 50% opacity white

  // Borders
  static const Color border = Colors.black12;
}
